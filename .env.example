# Generate distinct random hexadecimal passwords, at least 24 characters.
POSTGRES_PASSWORD=
OC_RUNTIME_PASSWORD=
OC_HTTP_PORT=8080

# Models default to disabled. Enabling them permits sending submitted content
# to the configured provider and may incur charges. API and Worker must agree.
OC_ENABLE_MODELS=false
OC_MODEL_BASE_URL=
OC_MODEL_API_KEY=
OC_EMBEDDING_MODEL=
OC_EMBEDDING_DIMENSION=0
OC_EXTRACTION_MODEL=
